# Dev Report

## 任务 ID

T-004 / T-005

## 分支 / worktree

- Branch: `codex/content-dev`
- Worktree: `/Users/black.chen/Desktop/nrg-worktrees/content-dev`

## 改动摘要

- T-004: Step 4 正文 textarea 的手动编辑现在会同步回父级 `article.content`; 点「下一步 · 段间配图」时用最新正文发起 `plan-images`, 后续 HTML 也继续使用编辑后的正文.
- T-005: 本次生成的段间图在 push 版 HTML 中加内部标记; `sanitize_for_push` 遇到带标记的 `?from=appmsg` mmbiz 图会保留图片并清理 URL/内部标记, 无标记历史图仍整剥.
- 追加 D-111 到公众号踩坑文档, 记录“本次上传段间图也可能带 `?from=appmsg`”。

## 改了哪些文件

- `web/factory-wechat-v2.jsx`
- `backend/services/wechat_scripts.py`
- `tests/test_wechat_sanitize.py`
- `tests/test_wechat_html_inject.py`
- `scripts/e2e_wechat_edit_propagation.js`
- `docs/WECHAT-SKILL-LESSONS.md`

## commit hash

- Code commit: `dcd5d3b`

## 已跑验证

- 新增 sanitizer 回归先在旧实现失败: `pytest -q tests/test_wechat_sanitize.py::test_current_section_image_with_appmsg_marker_kept_and_cleaned` -> failed before fix.
- `pytest -q tests/test_wechat_sanitize.py tests/test_wechat_html_inject.py` -> 31 passed.
- `APP='http://127.0.0.1:8102/?page=wechat' node scripts/e2e_wechat_edit_propagation.js` -> passed.
- 截图已读: `/tmp/_ui_shots/t004_wechat_edit_propagation.png`, HTML 预览含 `QA编辑标记-T004-手动正文必须进入后续流程`.
- `pytest -q tests/test_wechat_*.py tests/test_llm_empty_content.py tests/test_llm_retry.py` -> 101 passed.
- `curl POST http://127.0.0.1:8110/api/wechat/html` -> current worktree backend, `wechat_html` 含段间图内部标记且不含 `/media/`.
- 对 `/tmp/preview/wechat_article.html` 跑 `sanitize_for_push`: `img_count_original=3`, `img_count_sanitized=2`, 段间图保留, 头像 appmsg 剥除, 内部标记和 `from=appmsg` 均清掉.
- `git diff --check` -> passed.

## 没测到 / 需要 QA 重点测

- 没有再次真实推送公众号草稿, 避免副 Agent 重复外发; 交给 T-006 做 8 步真测回归.
- QA 重点看: Step 4 手动编辑标记是否进入 `plan-images`、Step 6 HTML、Step 8 推送前 HTML; `last_push_request.json` 的 `img_count_sanitized` 应至少保留 4 张正文段间图.
- 全量 `pytest -q -x` 在既有 `tests/test_integration.py::test_settings_loaded` 停下, 原因是当前 content-dev 环境没有 `SHILIU_API_KEY`; 与本次改动无关.

## 风险说明

- sanitizer 依赖 push 版 HTML 中的 `data-nrg-section-image="1"` 内部标记区分本次段间图; 已用当前 converter 的真实 `/api/wechat/html` curl 验证该标记会进入 `wechat_html`, 且 sanitize 后不会留在最终 HTML.
