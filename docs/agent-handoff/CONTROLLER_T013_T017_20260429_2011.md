# Controller Handoff

## 任务 ID

T-013 / T-017 / T-018 / T-019

## 本轮角色

- role: 总控 Agent
- worktree: `/Users/black.chen/Desktop/neironggongchang`
- branch: `main`

## 启动检查

- `pwd` -> `/Users/black.chen/Desktop/neironggongchang`
- `git branch --show-current` -> `main`
- `git status --short` -> 仅有既存未跟踪 `data/`、`vendor/`, 本轮未触碰。
- 已读:
  - `AGENTS.md`
  - `docs/MULTI_AGENT_WORKFLOW.md`
  - `docs/agents/ROLE_CONTROLLER.md`
  - `docs/AGENT_BOARD.md`
  - `docs/PROGRESS.md`
  - `docs/SYSTEM-CONSTRAINTS.md`
- `python3 scripts/agent_queue.py claim --role controller --agent "NRG 总控" --format prompt` -> 无 controller 任务。
- `python3 scripts/agent_inbox.py --hours 24` -> 53 reports。

## 已执行

### T-013 合入

- 没有整支 merge `codex/media-dev`, 避免带入落后分支删除多 Agent 基建。
- 只 cherry-pick:
  - `6e05558 fix: fail apimart tasks on download errors` (来自 `99f1fb3`)
  - `ff221fc docs: add media dev t013 handoff` (来自 `a700c0f`)

验证:

- `.venv/bin/pytest -q tests/test_apimart_service.py tests/test_remote_jobs.py` -> 21 passed。
- `.venv/bin/pytest -q -x` -> passed。
- `git diff --check HEAD~2..HEAD` -> clean。

状态:

- T-013 主线代码已合入并自验通过。
- 仍不能关闭 T-013; 已派 T-019 给 QA 做独立复测。

### 投流返修派工

收件箱证据:

- T-015 QA 报告: `docs/agent-handoff/QA_T015_TOULIU_N1_20260429.md`
- 结论: 投流 `n=1` 页面真烧 task `5984e0bcdb754ad994d4b65415bd901e` 724 秒后 Opus/OpenClaw timeout; console/pageerror=0; 页面失败态正常。

队列操作:

- 新增 T-017: 内容开发返修投流 `n=1` 真实页面链路 724s timeout。
- 新增 T-018: T-017 done 后 QA 做一次投流 `n=1` 页面最小真烧复测。
- 新增 T-019: QA 复测 T-013 主线合入后的 apimart 下载失败路径。

当前队列状态:

- T-017: `claimed` by `NRG 内容开发`
- T-018: `queued`, depends on T-017
- T-019: `claimed` by `NRG QA 自动`
- T-015/T-016: 保持 `blocked`, 避免投流未通过前继续烧录音/热点 credits。

## 需要下一任总控处理

1. 等 T-017 内容开发报告回来后, 读取收件箱并判断是否 reset/关闭旧 T-015, 或以 T-018 作为新的 QA 真烧入口。
2. 等 T-019 QA 报告回来后, 若证据完整且通过, 再把 T-013 标记完成。
3. T-018 真烧通过前, 不恢复 T-016 录音/热点真实链路。

## 是否需要老板确认

否。所有操作都在既有任务范围内:

- T-013 是已登记 fault injection 修复, 本轮只安全 cherry-pick 和自验。
- T-017/T-018 是 T-015 不通过后的返修/复测。
- T-019 不真烧 credits。
