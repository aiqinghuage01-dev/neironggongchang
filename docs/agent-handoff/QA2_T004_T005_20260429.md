# QA Report

## 任务 ID

T-004 / T-005

## 测试对象

分支 / commit / 页面:
- 被测分支: `codex/content-dev`
- 被测 commit: `4545a2b docs: add wechat content dev handoff`
- 代码 commit: `dcd5d3b fix: preserve wechat edits and section images`
- worktree: `/Users/black.chen/Desktop/nrg-worktrees/content-dev`
- 页面: `http://127.0.0.1:8111/?page=wechat`
- API: `http://127.0.0.1:8110`

## 真实操作

- 打开: 公众号 8 步链路页面 `?page=wechat`
- 点击: Step 4 `下一步 · 段间配图`, 段间图步骤 `真实感照片`, `一键生成 4 张`, `拼 HTML`
- 输入: 在 Step 4 textarea 追加 `QA编辑标记-T004-手动正文必须进入后续流程`
- 等待: Playwright 等到 Step 6 HTML 预览 iframe 出现编辑标记
- 复核: curl 当前 worktree 后端 `/api/wechat/html`, 再对返回的 `wechat_html` 调 `sanitize_for_push`

## 证据

- 截图:
  - `/tmp/_ui_shots/t004_wechat_edit_propagation.png`
- console error:
  - `scripts/e2e_wechat_edit_propagation.js` 内部监听 `console error`, 非 0 会失败；本轮退出码 `0`
- pageerror:
  - 同脚本监听 `pageerror`, 非 0 会失败；本轮退出码 `0`
- curl / 函数复核:
  - `POST /api/wechat/html` -> `wechat_has_edit_marker=true`, `wechat_has_marker=true`, `wechat_has_media=false`, `wechat_img_count=4`
  - `sanitize_for_push(wechat_html)` -> `original_img_count=5`, `clean_img_count=4`, `clean_has_from_appmsg=false`, `clean_has_internal_marker=false`, `clean_has_media=false`
  - sanitizer 统计: `removed.img_from_appmsg=1`, `rewritten.trusted appmsg kept=4`, `rewritten.strip ?from=appmsg=4`
- pytest:
  - `/Users/black.chen/Desktop/neironggongchang/.venv/bin/pytest -q tests/test_wechat_sanitize.py tests/test_wechat_html_inject.py` -> `31 passed`
  - `/Users/black.chen/Desktop/neironggongchang/.venv/bin/pytest -q tests/test_wechat_*.py tests/test_llm_empty_content.py tests/test_llm_retry.py` -> exit `0`, no failures
- static check:
  - `git diff --check` -> pass

## Credits / 外部服务

- 是否真烧: 否
- 测试规格: 本轮是 T-004/T-005 scoped QA, 只测编辑传播 + HTML/sanitizer; 未推送公众号草稿
- 输入参数:
  - Step 4 编辑标记: `QA编辑标记-T004-手动正文必须进入后续流程`
  - curl HTML 标记: `QA编辑标记-T004-QA复测`
- task id / 作品 id: 不适用
- 实际消耗: 无
- 是否重复提交: 否

## 结果

通过, 范围限定为 T-004/T-005。

- Step 4 手动编辑内容会进入 `/api/wechat/plan-images`
- Step 4 手动编辑内容会进入 `/api/wechat/html`
- Step 6 HTML 预览能看到编辑后的正文
- 4 张本次段间图在 push 前 sanitizer 后保留
- `from=appmsg` 和 `data-nrg-section-image` 内部标记会被清掉
- 无标记历史 appmsg 图仍会被剥除

未执行真实公众号草稿推送；这是 T-006 范围, 需要总控明确后再跑。

## 发现的问题

未发现 T-004/T-005 范围内的新问题。

## 复现步骤

1. 在 `/Users/black.chen/Desktop/nrg-worktrees/content-dev` 启动当前分支后端到 `8110`, 前端到 `8111`。
2. 运行 `APP='http://127.0.0.1:8111/?page=wechat' node scripts/e2e_wechat_edit_propagation.js`。
3. curl `POST http://127.0.0.1:8110/api/wechat/html`, body 带 4 张 `mmbiz_url` 且包含 `?from=appmsg`。
4. 对返回的 `wechat_html` 调 `backend.services.wechat_scripts.sanitize_for_push`。
5. 确认 4 张本次段间图保留, 历史头像类 appmsg 图剥除, 内部标记和 `from=appmsg` 清空。
