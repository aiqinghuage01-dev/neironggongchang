# QA Report

## 任务 ID

T-012 - 作品库 T-009/T-010/T-011 修复后全链路回归

## 测试对象

分支 / commit / 页面:

- 主线 worktree: `/Users/black.chen/Desktop/neironggongchang`
- 测试时主线 HEAD: `353e6e5 docs: mark imagegen t008 retest complete`
- 作品库修复提交: `9b36bd5 fix: repair works library qa issues`
- 作品库修复状态提交: `22472e0 docs: update works qa repair status`
- 说明: `22472e0..353e6e5` 后续差异只涉及 imagegen / docs / apimart 测试, 不涉及 `backend/api.py`、`shortvideo/works.py`、`web/factory-works.jsx`。
- 页面: `http://127.0.0.1:8131/?page=works`
- API: `http://127.0.0.1:8130`

## 真实操作

- 打开: 作品库页面, 通过 `localStorage.api_base=http://127.0.0.1:8130` 指向本轮测试 API。
- 点击: 时间筛选「全部」, 作品卡片, 「留这版」, 「各平台数据」, 数据看板排行「看」, 图片 tab。
- 输入: 搜索旧作品标题、动作作品标题、缺图作品标题; 完播率输入 `80` 并保存。
- 等待: 详情抽屉按 id 打开、按钮变为 `留这版 ✓`、指标保存返回、缺图文案渲染、网络空闲。

## 证据

- pytest:
  - `.venv/bin/pytest -q tests/test_works_api.py tests/test_works_crud_integration.py tests/test_autoinsert_text_work.py tests/test_migrations.py::test_apply_migrations_creates_works_indexes tests/test_migrations.py::test_legacy_fixup_works_old_db_missing_4_columns` -> `16 passed`
  - `.venv/bin/pytest -q -x` -> exit 0; 17 个 dhv5 相关 skip
- 回归脚本:
  - `APP='http://127.0.0.1:8131/?page=works' node scripts/e2e_works_t009_t011.js` -> exit 0
- 真实浏览器:
  - 非 mock Playwright 脚本打开真实 8131 页面, 对真实 8130 API 发起 32 个请求。
  - console error: `0`
  - pageerror: `0`
  - API requestfailed: `0`
- 截图已读:
  - `/tmp/_ui_shots/t012_works_search_old.png` - 旧作品搜索命中并打开 `作品详情 #446`
  - `/tmp/_ui_shots/t012_works_keep_metrics.png` - `留这版 ✓` 可见, 各平台数据 tab 显示已保存抖音数据
  - `/tmp/_ui_shots/t012_works_analytics_open.png` - 数据看板排行通过「看」打开 `作品详情 #751`
  - `/tmp/_ui_shots/t012_works_missing_image.png` - 图片卡和详情均显示「原图不在本机 / 原图文件不在本机」
- curl:
  - `GET /api/works?q=T012 old search needle t012-1777444377&limit=300` -> `count=1`, `ids=[446]`, `found_old=true`
  - `GET /api/works/446` -> `has_full_text=true`
  - `GET /api/works/752` -> `metadata.user_action=kept`
  - `GET /api/works/752/metrics` -> `platform=douyin`, `completion_rate=0.8`
  - `GET /api/works/analytics` -> `top_by_views[0].work_id=751`, `contains_old=true`
  - `GET /api/works/753` -> `asset_status=missing_file`, `preview_available=false`, `download_available=false`, `local_url=null`, `thumb_url=null`
- 测试数据清理:
  - 本轮创建 `t012-1777444377` 临时作品 id `446..753`, 共 308 条。
  - 删除指标 2 条、作品 308 条。
  - 清理后 `GET /api/works?q=t012-1777444377&limit=10000` -> 剩余 `0`。

## Credits / 外部服务

- 是否真烧: 否
- 测试规格: 作品库本地 API / UI 回归, 未触发数字人、生图、公众号推送。
- 输入参数: `t012-1777444377` 唯一测试标记。
- task id / 作品 id: 临时作品 `446..753`, 指标 `4,5`, 已清理。
- 实际消耗: 无生成 credits 消耗。
- 是否重复提交: 否。

## 结果

通过 T-012 作品库回归。只覆盖作品库 T-009/T-010/T-011 修复, 不代表项目整体通过。

## 发现的问题

未发现新的 P0/P1/P2 问题。

## 复现步骤

1. 启动主线 API: `uvicorn backend.api:app --host 127.0.0.1 --port 8130`
2. 启动主线 Web: `python3 scripts/start_web_nocache.py 8131`
3. 浏览器打开 `http://127.0.0.1:8131/?page=works`, 设置 `localStorage.api_base=http://127.0.0.1:8130`。
4. 用唯一标记创建旧作品、排行作品、动作作品、缺图作品和填充作品, 再按「真实操作」逐项验证。
5. 测完删除本轮唯一标记对应的作品和指标。

## 备注

- 主线 worktree 仍有未跟踪 `data/`、`vendor/`。
- QA worktree 仍有未跟踪 `data/`。
- 本报告不改业务代码, 不更新 `docs/PROGRESS.md`。
