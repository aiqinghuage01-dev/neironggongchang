[24h-audit cron tick · launchd 触发] cwd 应为 /Users/black.chen/Desktop/neironggongchang.

起手三连:
1. git status --short (看上次 tick 残留)
2. cat docs/design/audit/PROGRESS.md (找 in_progress / 拉下一个 pending task)
3. 按 docs/design/audit/CRON_PROMPT.md 流程跑一个 stage

tick 末尾必须:
- git add 改动文件 + commit (commit msg 加 [24h-audit] 前缀)
- 更新 PROGRESS.md 的 status / stage / 历史 tick 日志

每次 tick 是独立 session, 状态全在 PROGRESS.md / git log 里. 不要试图重读对话上下文, 直接按 PROGRESS.md 续上即可.

特权: 清华哥本次明确授权 — 直接动 web/factory-*.jsx 代码, 模拟本人按 docs/design/audit/SELF_REVIEW.md 8 项审核 (PASS 才 commit). 出门了, 中午不打扰. 24h 截止 2026-04-26 23:31 CST 后写 docs/design/audit/SUMMARY.md, launchd 会自动卸载 plist 自停.

decision 卡壳时: 用 SELF_REVIEW + ~/.claude/projects/-Users-black-chen-Desktop-BLACK-claudecode/memory/ feedback 自决, 不留 todo 给清华哥. 标 blocked 是最后手段 (3 次 revise 没过才标).

时间预算: 一个 tick ~20 min, 一个 stage 没做完不要硬撑, 标 in_progress + commit WIP + exit, 下个 cron 续.

禁止动: backend/**.py / pyproject.toml / requirements.txt / .claude/** / settings.json / cosyvoice_* / vendor/ / data/.
允许动: web/factory-*.jsx (当前 task 的 page) / docs/design/audit/** / git add commit.
